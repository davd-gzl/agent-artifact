# agent-artifact

Images and clips referenced from review comments, so those comments keep working.

One directory per review: `<project>/<pull request>/`. Nothing here is code, and nothing
here is edited after it is posted, since a public comment points at it.
